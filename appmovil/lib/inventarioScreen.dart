import 'package:flutter/material.dart';

class InventarioScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pantalla de Inventario'),
      ),
      body: Center(
        child: Text('Contenido de la pantalla de Inventario'),
      ),
    );
  }
}
